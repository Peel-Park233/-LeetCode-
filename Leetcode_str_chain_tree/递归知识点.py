# 递归算法的三个要素：
# 1.确定递归函数的参数和返回值：
#   确定哪些参数是递归的过程中需要处理的，那么就在递归函数里加上这个参数，
#   并且还要明确每次递归的返回值是什么进而确定递归函数的返回类型。
# 2.确定终止条件：
# 3.确定单层递归的逻辑： 确定每一层递归需要处理的信息。
# print(2 + int([1, 2, 3, 4]))
A = [1, 2, 3, 4,  5]
k = [1 + num for num in A]
print(k)
